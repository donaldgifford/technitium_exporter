# CHANGELOG.md is generated by git-cliff and verified byte-for-byte by the
# changelog drift check in CI. prettier's proseWrap would reflow it and make
# every PR fail. Regenerate with `just changelog`, never format it.
CHANGELOG.md

# Vendored / generated output
dist/
build/

# Machine-managed Claude Code session state (gitignored), not prose
.claude/
